# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=easyfactu
DB_USER=postgres
DB_PASSWORD=Bruno1234.

# Database Pool Configuration
DB_POOL_MAX=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# Server Configuration
PORT=3000
NODE_ENV=development

# Authentication (JWT)
# IMPORTANT: Change this secret to a long random string in production!
JWT_SECRET=change_this_to_a_long_random_secret_in_production
JWT_EXPIRES_IN=8h

# CORS – comma-separated list of allowed frontend origins.
# The Vite dev-server usually runs on port 5173 or 5174.
CORS_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:4173

# AEAT Configuration (for production)
AEAT_URL=https://prewww1.aeat.es/wlpl/TIKE-CONT/ValidarRegFactuSistemaFacturacion
AEAT_CERT_PATH=
AEAT_CERT_PASSWORD=
